# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: nathan.contact::lang.models.message.id
        searchable: true

    name:
        label: nathan.contact::lang.models.message.name
        searchable: true

    email_address:
        label: nathan.contact::lang.models.message.email_address
        searchable: true

    phone_number:
        label: nathan.contact::lang.models.message.phone_number
        searchable: true

    created_at:
        label: nathan.contact::lang.models.message.created_at
        type: datetime
        searchable: true